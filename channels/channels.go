package main

import "fmt"

func main() {
	/*
	var c= make(chan int)
	c <- 1
	i := <-c
	fmt.Println(i)
	unbuffered channel and deadlock
	why ? 
	well 
	it waits for someone (fn) to receive 
	well there's no other go routine so it just waits forever like how you waited for her 
	*/
}
